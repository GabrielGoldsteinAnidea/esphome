substitutions:
  name: air-alarm2
  friendly_name: airalarm2

esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  name_add_mac_suffix: false
  platformio_options:
    board_build.mcu: esp32s2
    board_build.variant: esp32s2

packages:
  - !include air-alarm-common.yaml
